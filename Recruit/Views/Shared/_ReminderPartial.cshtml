@model Candidate
@{
    List<SelectListItem> owners = new List<SelectListItem>
        ();
    @foreach (var items in (IEnumerable<Owner>)TempData["Owners"])
    {
        owners.Add(new SelectListItem
        {


            Text = items.first_name,
            Value = items.id.ToString(),
            Selected = true

        });
    }
}

<div class="column">
    <label class="control-label1">Owner For Reminder</label>
    @Html.DropDownListFor(model => model.owner_for_reminder_id, new SelectList(owners, "Value", "Text"), "---Choose Status---", new { @class = "form-control" })
    <span asp-validation-for="owner_for_reminder_id" class="text-danger"></span>
</div>